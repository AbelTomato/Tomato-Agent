# Project Rules

## Task Completion Logging

每完成一轮任务，必须先确认当前时间，并使用 `Asia/Shanghai` 时区，将本轮用户发送的每一条 Prompt 原文、执行过程、修改内容和验证结果写入 `docs/development-log/YYYY-MM-DD.md`。

- Prompt 必须记录用户实际发送的原文，不得只记录摘要、需求拆解或改写版本。
- Prompt 中的 Markdown、代码块、列表、约束和需求调整必须保留。
- 同一轮中的多条用户消息必须按发送顺序分别记录。
- 需求澄清、补充和调整必须作为独立 Prompt 原文记录。
- 需求拆解和执行摘要可以额外记录，但不能替代 Prompt 原文。
- 如果无法取得完整原文，必须标记 `PROMPT_NOT_AVAILABLE`，不得自行补写为原文。

## Manual Code Review

每完成一轮任务，Agent 必须单独列出“重点需 CR 的文件”，使用绝对路径，并为每个文件说明建议 CR 的重点。重点覆盖安全边界、状态隔离、异常处理、接口兼容性和测试覆盖；不要求罗列所有修改文件。

- Agent 只能执行并记录“Agent 自检”，不得将其称为人工 CR。
- 人工 CR 必须由项目使用者执行。
- 未收到用户 CR 反馈前，开发日志必须标记“人工 CR：待用户执行”。
- 用户完成 CR 并反馈结果后，Agent 才能将用户反馈追加到开发日志。
- Agent 不得声称已经完成用户人工 CR，也不得代替用户批准修改。

## Testing Acceptance

代码编写完成后必须编写或更新测试并运行。测试通过才可作为验收基准；无法运行时必须明确说明原因，不得声称已通过。必须说明测试逻辑。

## Development Documentation

每轮开发必须记录用户 Prompt、需求拆解、问题解决流程、方案选择、修改文件、测试命令、测试结果和遗留风险。

## Agent Runtime Boundary

- Agent Runtime 的核心 Loop 由项目使用者手动实现，本项目不得代写其决策逻辑。
- 不得使用 LangChain 或其他 Agent 框架替代核心 Loop。
- 真实 LLM API 由使用者通过 `LLMClient` 接口接入。
- 本项目只提供 Runtime 所需的模型、Session、Context、Tool、Trace 和持久化基础设施。

## Security

- `calculator` 禁止使用 `eval`/`exec`。
- `read_docs` 必须执行路径 allowlist 校验，拒绝路径穿越。
- 工具必须限制输入、输出和执行时间。
- 日志不得写入 API Key 或 Authorization Header。