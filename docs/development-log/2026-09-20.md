# 2026-09-20 开发日志

## 23:24 任务 5：知识问答检索、追问与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的三种检索模式分派、追问查询改写、空结果处理和引用快照生成；知识问答 API 保存原问题、检索问题、回答、检索模式和文档版本快照。
- **遇到问题**：恢复上下文时需要重新核对服务测试替身及 API 注入边界；未形成生产阻塞。
- **解决方式**：确认向量/混合检索通过注入查询 Embedding 执行，追问改写失败显式报错；历史回答使用事件中保存的引用快照，不重新读取当前文档。
- **验证结果**：`tests/test_knowledge_service.py tests/test_knowledge_api.py -q` 为 **42/42 通过**；知识服务和 API `compileall` 通过；未调用真实 LLM 或 Embedding 服务。

## 22:59 任务 5：知识问答服务与追问 API 阶段

- **完成内容**：完成 `KnowledgeService` 的三种检索模式分派、查询向量边界校验、JSON 回答校验、引用归属校验和追问改写；知识问答 API 保存原问题、检索问题、检索模式、回答和引用快照，引用保留文档版本与正文快照。
- **遇到问题**：恢复上下文时测试替身的方法归属曾需重新核对；未形成生产代码阻塞。
- **解决方式**：重新读取服务、Session 仓储和 API 路由，确认向量方法属于 `FakeKnowledgeRepository`；追问改写失败、非法 JSON 和空改写均显式返回错误，不静默使用原问题。
- **验证结果**：`tests/test_knowledge_service.py tests/test_knowledge_api.py -q` 为 **42/42 通过**；相关 `compileall` 通过；未调用真实 LLM 或 Embedding 服务。

## 22:21 任务 5：查询 Embedding 边界校验阶段

- **完成内容**：强化 `KnowledgeService.retrieve()` 的查询向量校验；向量/混合模式现在拒绝空向量、维度不匹配、布尔值、非数值、`NaN` 和无穷值，并在校验通过后统一转换为浮点列表。
- **遇到问题**：服务层注入的查询 Embedding 可绕过 HTTP Embedding 客户端已有的有限数值校验。
- **解决方式**：在服务层增加独立边界校验，并验证非法向量不会传入向量或混合仓储。
- **验证结果**：`tests/test_knowledge_service.py` **32/32 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 22:13 任务 5：知识问答服务生成回答与引用约束阶段

- **完成内容**：完成 `KnowledgeService.answer()` 的离线生成流程；检索结果作为不可信证据注入提示词，模型输出必须为 JSON，且引用 ID 只能来自本次检索结果；无检索结果时直接返回 `no_results`，不调用模型。
- **遇到问题**：恢复上下文时发现服务测试替身的方法归属需要重新核对，但生产实现已存在且完整。
- **解决方式**：重新读取服务实现和测试文件，确认 `FakeKnowledgeRepository` 提供三种检索方法，未重复修改生产逻辑。
- **验证结果**：`tests/test_knowledge_service.py` **26/26 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 22:17 任务 5：知识问答服务检索分派与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；通过可注入查询 Embedding 执行向量模式；检索结果转换为冻结的 `CitationSnapshot`，保留文档版本、来源、章节、行号和正文快照；空结果不生成引用。
- **遇到问题**：恢复上下文时发现测试替身方法归属需要重新核对，未形成生产代码阻塞。
- **解决方式**：确认向量和混合方法属于 `FakeKnowledgeRepository`，修正后未修改生产仓储接口。
- **验证结果**：`tests/test_knowledge_service.py` **26/26 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 22:03 任务 5：知识问答服务检索分派与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；使用可注入查询 Embedding，向量模式不静默降级；检索结果转换为冻结的 `CitationSnapshot`，保存文档版本、来源、章节、行号和正文快照。
- **遇到问题**：恢复执行时曾误判服务层尚未实现，实际生产代码已具备该方法；测试夹具方法归属需重新核对。
- **解决方式**：重新读取服务和测试完整内容，确认向量/混合方法属于 `FakeKnowledgeRepository`，未重复修改生产逻辑。
- **验证结果**：`tests/test_knowledge_service.py` **26/26 通过**；相关编译检查通过；未调用真实模型或外部 Embedding 服务。

## 21:53 任务 5：知识问答服务检索分派与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；向量模式使用可注入查询 Embedding，禁止静默降级；检索结果转换为冻结的 `CitationSnapshot`，无结果返回 `no_results`。
- **遇到问题**：测试夹具中曾残留未闭合的向量仓储方法定义，导致服务测试语法错误。
- **解决方式**：清理残留定义并将向量/混合方法归回 `FakeKnowledgeRepository`，未修改生产仓储接口。
- **验证结果**：`tests/test_knowledge_service.py` **26/26 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 21:07 任务 5：知识问答服务检索分派与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；向量模式使用可注入查询 Embedding，禁止静默降级；检索结果转换为冻结的 `CitationSnapshot`，无结果返回 `no_results`。
- **遇到问题**：新增服务测试时，向量仓储方法暂时插入了 `FakeLLM`，导致服务测试收集/分派失败。
- **解决方式**：将向量和混合方法归回 `FakeKnowledgeRepository`，未修改生产接口。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；相关编译检查通过；未调用真实模型或外部 Embedding 服务。

## 21:04 任务 5：知识问答服务检索与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的三种检索模式分派；通过注入查询 Embedding 执行向量/混合检索，关键词模式不调用 Embedding；将结果转换为冻结的 `CitationSnapshot`，保存文档版本、片段、来源、章节、行号和正文快照；无结果返回 `no_results`。
- **遇到问题**：测试夹具中的向量仓储方法初始归入错误的替身类，导致服务测试无法按预期分派。
- **解决方式**：将向量和混合检索方法归回 `FakeKnowledgeRepository`，未修改生产仓储接口。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 20:54 任务 5：知识问答服务与 API 阶段门恢复验证

- **完成内容**：复核 `KnowledgeService.retrieve()` 与 `answer()`、三种检索模式分派、引用快照校验及知识问答 HTTP 入口；确认空结果不会调用模型，引用只能来自本次检索结果。
- **遇到问题**：恢复上下文时曾误判测试夹具中的向量仓储方法归属异常。
- **解决方式**：重新读取完整测试文件和生产实现，确认当前夹具与生产代码均已正确，无需重复修改。
- **验证结果**：`tests/test_knowledge_service.py tests/test_knowledge_api.py tests/test_main.py` **33/33 通过**；`app`、知识服务和 API 测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 20:42 任务 5：知识问答检索服务与引用快照阶段验证

- **完成内容**：验证 `KnowledgeService.retrieve()` 对关键词、向量和混合检索的分派；确认查询 Embedding 注入、空结果短路、检索模式校验和冻结 `CitationSnapshot` 已生效。
- **遇到问题**：恢复上下文时曾误判服务方法尚未实现，并发现测试夹具位置需要重新核对。
- **解决方式**：重新读取服务与测试文件，确认生产实现和 `FakeKnowledgeRepository` 归属均正确；未重复修改既有逻辑。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 20:38 任务 5：知识问答检索服务阶段恢复与验证

- **完成内容**：恢复并核对 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；确认查询 Embedding 注入、无结果短路和冻结引用快照行为已实现。
- **遇到问题**：恢复上下文时测试夹具位置曾被误判，可能将仓储方法与 LLM 替身混淆。
- **解决方式**：重新读取测试文件精确核对类归属，确认当前夹具无须生产代码修改。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 20:28 任务 5：知识问答检索服务阶段恢复与夹具清理

- **完成内容**：恢复并核对 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；清理服务测试中重复的 Fake 仓储方法，保留查询 Embedding 注入、引用快照和无结果短路行为。
- **遇到问题**：清理重复方法时误删测试夹具的关键词检索返回行，导致 12 个服务测试因夹具初始化失败。
- **解决方式**：恢复 `search_chunks()` 的异步方法和返回值，重新核对 Fake 仓储方法归属；未修改生产服务逻辑。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 20:06 任务 5：知识问答 HTTP 检索入口

- **完成内容**：新增 `POST /api/sessions/{session_id}/knowledge-runs`，支持问题、检索模式和结果数量校验；复用现有 Session 校验和 SQLite，返回结构化答案、证据状态及引用快照；知识仓储与 Session 仓储在应用生命周期中初始化。
- **遇到问题**：HTTP 测试替身使用 `answer` 实例属性覆盖了异步 `answer()` 方法，导致测试请求出现类型错误。
- **解决方式**：将测试替身属性改名为 `result`，保持生产服务接口不变。
- **验证结果**：知识 API、服务契约及既有主 API 共 **33/33 通过**；`app` 与知识 API/服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 20:03 任务 5：知识问答服务检索模式与引用快照

- **完成内容**：完成 `KnowledgeService.retrieve()` 的离线检索服务切片，统一支持关键词、向量和混合模式；向量模式通过注入查询 Embedding 函数获取查询向量，禁止静默降级；检索结果转换为冻结的 `CitationSnapshot`，保存文档版本、片段、来源、章节、行号和正文快照；无结果返回 `no_results` 且不产生引用。
- **遇到问题**：测试夹具中的向量仓储方法初始插入到错误的类，导致向量模式测试无法调用预期接口。
- **解决方式**：将测试方法归回 `FakeKnowledgeRepository`，保持生产仓储接口不变。
- **验证结果**：`tests/test_knowledge_service.py` 为 **25/25 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 19:58 任务 5：知识问答服务检索、生成与引用校验

- **完成内容**：新增 `KnowledgeService.retrieve()` 与 `answer()`；统一支持关键词、向量和混合检索，向量查询通过注入 Embedding 函数执行；无结果时不调用模型；有结果时使用不可信材料提示词调用注入的 LLM，并通过 `GeneratedKnowledgeAnswer` 限制引用只能来自本次检索结果；引用继续使用包含文档版本、片段、URL、章节、行号和正文快照的冻结模型。
- **遇到问题**：服务测试夹具中的向量仓储方法误插入 `FakeLLM`，导致向量模式测试无法调用仓储；`KnowledgeService.answer()` 尚未实现时，既有答案契约测试出现失败。
- **解决方式**：将向量方法移回 `FakeKnowledgeRepository`；实现检索、无结果短路、JSON 生成提示和严格引用 ID 校验流程。
- **验证结果**：`tests/test_knowledge_service.py` 为 **25/25 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 19:51 任务 5：知识问答检索服务模式与引用快照

- **完成内容**：实现 `KnowledgeService.retrieve()`，统一执行关键词、向量和混合检索；向量模式通过注入查询 Embedding 函数获取查询向量，不进行静默降级；检索结果转换为冻结的 `CitationSnapshot`，保存文档版本、片段、来源、章节、行号和正文快照。
- **遇到问题**：无；恢复时发现服务层和向量仓储接口已存在，未重复修改已有实现。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_knowledge_service.py` 为 **20/20 通过**；服务层未调用真实模型或外部 Embedding。

## 19:48 任务 5：知识问答检索服务与引用快照

- **完成内容**：新增 `KnowledgeService.retrieve()`，统一支持关键词、向量和混合三种检索模式；通过注入查询 Embedding 函数执行向量查询；将检索结果转换为冻结的 `CitationSnapshot`，保存文档版本、片段、URL、章节、行号和正文快照。
- **遇到问题**：无；服务层测试使用本地 Fake Repository 和人工查询向量。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_knowledge_service.py` 为 **20/20 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 19:11 任务 4：向量检索阶段门验证

- **完成内容**：完成 Embedding 协议、关键词/余弦向量/RRF 检索、SQLite 向量元数据绑定和文档版本隔离的阶段验证，确认当前实现满足任务 4 的离线验收边界。
- **遇到问题**：无。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_embeddings.py tests/test_retrieval.py tests/test_knowledge_repository.py tests/test_knowledge_ingestion.py -q` 为 **34/34 通过**；相关 `compileall` 通过；未调用真实外部服务。

## 18:47 任务 4：向量检索阶段门验证

- **完成内容**：复核并验证 Embedding 协议、SQLite 向量持久化、文档版本绑定、余弦向量检索和 RRF 混合排序；修正知识库说明中关于向量检索/RRF“尚未实现”的过期描述。
- **遇到问题**：知识库说明前后存在一处实现状态不一致。
- **解决方式**：以当前代码和目标测试为准回填说明，保留“未调用真实 Embedding、未实现摘要和 Agent 接入”的限制。
- **验证结果**：`tests/test_embeddings.py tests/test_retrieval.py tests/test_knowledge_repository.py tests/test_knowledge_ingestion.py -q` 为 **34/34 通过**；相关 `compileall` 通过。

## 18:35 任务 4：向量持久化、余弦检索与 RRF 阶段恢复验证

- **完成内容**：恢复并核对 SQLite `embeddings` 表、向量元数据绑定、文档替换时旧向量清理、余弦向量检索和 RRF 去重排序实现；未重复修改已存在且符合要求的代码。
- **遇到问题**：无。
- **解决方式**：运行向量持久化、关键词检索、余弦检索、RRF、导入和 Embedding 协议目标测试，确认实现状态与任务 4 验收点一致。
- **验证结果**：`tests/test_retrieval.py tests/test_knowledge_repository.py tests/test_knowledge_ingestion.py tests/test_embeddings.py` 共 **34/34 通过**。

## 项目级 `.clinerules` 与开发文档路径规范

- **完成时间**：13:47（Asia/Shanghai）
- **完成内容**：创建项目级 `.clinerules`；将开发日志路径规范确定为 `docs/development-log/`，按 `YYYY-MM-DD.md` 命名；将实施计划统一约束到 `docs/实施计划/`，并要求一级标题注明计划日期。
- **遇到问题**：初始需求中的 `development-logs/` 为目录名笔误，项目现有目录实际为 `docs/development-log/`。
- **解决方式**：按用户修正后的要求使用现有单数目录，不创建复数目录；创建当日日志文件记录本次变更。
- **验证结果**：已创建并写入 `.clinerules` 与 `docs/development-log/2026-09-20.md`；规则内容覆盖日志路径、日期文件名、时间顺序、24 小时制时间、四项日志字段及实施计划路径和标题日期要求。

## 个人博客 RAG 与 Agent：任务 1 会话历史与串行调用边界

- **完成时间**：15:19（Asia/Shanghai）
- **完成内容**：新 Run 继承同一 Session 内已完成 Run 的用户消息和最终回答；恢复 Run 仅回放当前 Run；Context 按完整工具调用/结果组裁剪；兼容客户端显式请求串行工具调用并拒绝 Provider 返回的多个工具调用。补充 Runtime、Repository、HTTP 集成与客户端回归测试，并更新系统设计。
- **遇到问题**：测试工具的多文件参数不兼容，且一次补充验证命令在项目根目录使用了错误的虚拟环境相对路径。
- **解决方式**：改用项目指定的后端虚拟环境绝对路径运行 pytest；错误命令未修改文件，随后使用正确路径完成文档更新与验证。
- **验证结果**：基线 `tests/test_runtime.py tests/test_context.py tests/test_compatible_client.py` 为 26/26 通过；最终 `tests/test_repository.py tests/test_main.py tests/test_runtime.py tests/test_context.py tests/test_compatible_client.py -q` 为 49/49 通过；`python -m compileall -q app tests` 通过。

## 个人博客 RAG 与 Agent：任务 2 语料清单与评测规范

- **完成时间**：15:37（Asia/Shanghai）
- **完成内容**：新增 Redis 与 Python 缓存固定 Markdown 语料、显式 JSON 清单、离线数据集校验测试和 RAG 评测规范；语料包含中文标题、API 标识符、代码围栏、同名章节及不可信伪指令。
- **遇到问题**：初次基线检查因任务 2 的测试文件尚未创建而无法收集用例。
- **解决方式**：按计划新增测试资产及校验逻辑；正式导入器将在任务 3 实现，当前校验仅作为离线数据集契约。
- **验证结果**：`/home/abeltomato/workspace/projects/Tomato-Agent/backend/.venv/bin/python -m pytest tests/test_knowledge_dataset.py -q` 为 9/9 通过，覆盖有效清单、重复路径、目录穿越、非法 URL、缺失文档和无效证据行范围。

## 个人博客 RAG 与 Agent：任务 3 Markdown 分块与可重复导入

- **完成时间**：16:03（Asia/Shanghai）
- **完成内容**：新增知识库模型、UTF-8 Markdown 解析与分块、SQLite 文档/片段仓储、本地导入与安全同步 CLI，以及知识库使用说明；导入使用稳定路径身份、内容版本与单文档事务替换，Embedding 索引状态初始为待更新。
- **遇到问题**：补充的面经逐字稿实际为 DOCX/TXT，不符合本任务仅支持 Markdown 的范围；超长普通段落边界测试暴露了分块内部结构错误。
- **解决方式**：未读取或导入逐字稿正文，明确后续以独立私有适配任务处理；修正普通段落的分块结构，并保留 front matter 为普通正文、不解释其元数据。
- **验证结果**：`tests/test_knowledge_ingestion.py tests/test_knowledge_repository.py tests/test_repository.py -q` 为 25/25 通过；`python -m compileall -q app/knowledge tests/test_knowledge_ingestion.py tests/test_knowledge_repository.py` 通过。

## 个人博客 RAG 与 Agent：公开博客清单 URL 映射

- **完成时间**：16:58（Asia/Shanghai）
- **完成内容**：根据博客项目的 Astro Content 路由和 `github-slugger@2.0.0` 规则，为 `backend/knowledge-manifests/public-blog-manifest.json` 的 66 篇 `.md` 文章补全公开 URL；2 篇 `.mdx` 文章因本任务仅支持 Markdown 未纳入。
- **遇到问题**：原有 `dist/sitemap-0.xml` 比当前博客源码旧，首次核对缺少新增文章；直接导入顶层 `github-slugger` 也因 pnpm 依赖未提升而失败。
- **解决方式**：先执行博客项目 `pnpm exec astro check --minimumSeverity error`，再执行 `pnpm build` 生成最新 sitemap；从博客项目实际安装的 `github-slugger@2.0.0` 路径加载同一实现，完成源码路径、生成 ID 和 sitemap 的全量核对。未修改博客项目源码或依赖。
- **验证结果**：博客项目 `astro check` 为 0 errors，`pnpm build` 成功构建 133 个页面；66 条 `.md` 清单 URL 全部匹配最新 sitemap，路径唯一、标题非空、URL 非空且为 `https://abeltomato-blog.vercel.app/blog/.../`。

## 个人博客 RAG 与 Agent：公开博客临时 SQLite 导入

- **完成时间**：17:11（Asia/Shanghai）
- **完成内容**：使用已确认 URL 的 66 篇公开 Markdown 清单，将博客内容导入临时数据库 `/tmp/tomato-public-blog.db`；生成 66 个文档和 927 个片段，所有文档 URL 均已保存，索引状态保持为 `pending`。
- **遇到问题**：首次统计脚本错误读取不存在的 `Document.index_status` 属性。
- **解决方式**：不修改业务代码，改用 SQLite 只读查询直接统计表字段；错误脚本未影响导入结果。
- **验证结果**：首次导入 `succeeded=66 skipped=0 updated=0 failed=0`；重复导入 `succeeded=66 skipped=66 updated=0 failed=0`；数据库统计为 `documents=66`、`chunks=927`、`missing_urls=0`，代表性片段的标题路径、行号和 URL 核对通过。

## 个人博客 RAG 与 Agent：确定性关键词检索与引用结果

- **完成时间**：17:17（Asia/Shanghai）
- **完成内容**：新增 `SearchResult` 引用模型和 `KnowledgeRepository.search_chunks(query, limit)`；实现大小写折叠、空白归一化、标题/标题路径/正文加权排序、稳定并列排序，以及文档版本、URL、章节和行号返回。
- **遇到问题**：初始知识库说明仍描述“尚未执行真实数据库导入”，且缺少当前检索接口边界说明。
- **解决方式**：回填说明文档，明确 66 篇公开 Markdown 已导入临时数据库，并标注当前仅为关键词基线，未实现 BM25、Embedding、RRF 或 Agent 接入。
- **验证结果**：先以 2 个检索测试确认缺少 API 的预期失败，再实现代码；目标测试 `tests/test_knowledge_repository.py tests/test_knowledge_ingestion.py` 为 16/16 通过，`compileall` 通过。

## 个人博客 RAG 与 Agent：任务 4 关键词 BM25 与 Embedding 协议基线

- **完成时间**：18:10（Asia/Shanghai）
- **完成内容**：新增字段加权 BM25 关键词检索器，支持英文标识符分段、中文相邻双字和稳定排序，仓储检索接口保持引用元数据；新增独立 Embedding 配置及 OpenAI-compatible `/embeddings` 客户端，校验批量返回顺序、索引、维度和有限浮点数，未接入真实服务或向量存储。
- **遇到问题**：初版 token 规则未命中 `cachetools.TTLCache` 的后缀查询，且中文查询会因单个常见双字产生误命中；`httpx.Response(json=...)` 不能构造 NaN/Infinity 测试响应。
- **解决方式**：保留完整英文标识符并追加 `.`、`_`、`-` 分段 token，要求中文查询的全部相邻双字匹配同一片段；对非有限数值改用原始 JSON 响应体进行 MockTransport 验证。
- **验证结果**：先确认 `retrieval.py` 和 `embeddings.py` 缺失时的预期 RED，再完成实现；`tests/test_embeddings.py tests/test_retrieval.py tests/test_knowledge_repository.py tests/test_knowledge_ingestion.py -q` 为 27/27 通过，相关 `compileall` 通过；临时公开博客库只读检索 `FastAPI 依赖注入` 返回 3 条带 URL、章节和行号的结果。

## 个人博客 RAG 与 Agent：Embedding 向量持久化与版本绑定

- **完成时间**：18:23（Asia/Shanghai）
- **完成内容**：新增 `EmbeddingRecord`；SQLite 新增 `embeddings` 表及模型、维度、文档版本、文本哈希和向量元数据；仓储新增向量写入/读取接口，重复键更新；文档替换事务先清理旧向量，避免新文本继续使用旧向量。
- **遇到问题**：首次补丁因测试文件导入上下文与预期不一致未应用；补丁未产生文件修改。
- **解决方式**：重新读取准确上下文后拆分补丁，按模型、仓储、测试顺序完成实现；写入前校验向量维度、模型、文本哈希和有限数值，并校验向量对应现有分块版本。
- **验证结果**：`tests/test_knowledge_repository.py -q` 为 11/11 通过；`python -m compileall -q app/knowledge tests/test_knowledge_repository.py` 通过；覆盖向量元数据保存、同键更新、文档版本替换清理和维度不匹配拒绝。

## 个人博客 RAG 与 Agent：余弦向量检索与 RRF 混合排序

- **完成时间**：18:31（Asia/Shanghai）
- **完成内容**：新增离线余弦向量检索、查询向量有限值/维度校验、模型与维度不兼容时的 `IndexRebuildRequired`；新增 RRF 去重和稳定排序；仓储新增向量检索与关键词+向量混合检索入口。
- **遇到问题**：无外部阻碍；测试使用人工构造向量，未调用 Embedding 服务。
- **解决方式**：向量检索只接受与索引模型、维度一致且文档版本匹配的记录；RRF 按 `chunk_id` 合并，保持引用元数据并使用路径、行号和片段 ID 稳定打破并列。
- **验证结果**：`tests/test_retrieval.py tests/test_knowledge_repository.py -q` 为 17/17 通过；`python -m compileall -q app/knowledge tests/test_retrieval.py tests/test_knowledge_repository.py` 通过；覆盖余弦排序、RRF 去重、混合检索和重建索引错误。

## 个人博客 RAG 与 Agent：向量持久化阶段验收

- **完成时间**：18:59（Asia/Shanghai）
- **完成内容**：复核并验收 `EmbeddingRecord`、SQLite `embeddings` 表、向量元数据绑定、同键更新、文档版本替换清理，以及仓储向量/混合检索入口；确认旧向量不会跨文档版本继续使用。
- **遇到问题**：无；本阶段未调用真实 Embedding 服务。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_embeddings.py tests/test_retrieval.py tests/test_knowledge_repository.py tests/test_knowledge_ingestion.py -q` 为 34/34 通过；相关 `compileall` 通过。

## 个人博客 RAG 与 Agent：知识问答检索服务与引用快照

- **完成时间**：19:36（Asia/Shanghai）
- **完成内容**：新增 `KnowledgeService.retrieve()`，统一支持关键词、向量和混合三种检索模式；向量模式通过注入的查询 Embedding 函数执行，不隐式调用外部服务；检索结果转换为冻结的 `CitationSnapshot`，保存文档版本、片段、URL、章节、行号和正文快照。
- **遇到问题**：无；服务层测试使用本地 Fake Repository 和人工查询向量。
- **解决方式**：无需处理。
- **验证结果**：知识服务、检索、仓储和导入目标测试 `39/39` 通过；相关 `compileall` 通过；覆盖三种检索模式、空结果、空查询、未知模式、缺少查询 Embedding 和引用快照不可变性。

## 个人博客 RAG 与 Agent：任务 5 检索模式服务阶段恢复验证

- **完成时间**：20:14（Asia/Shanghai）
- **完成内容**：恢复并核对 `KnowledgeService.retrieve()`、三种检索模式分派、查询 Embedding 注入和 `CitationSnapshot` 引用快照逻辑；确认向量模式不静默降级，空结果不生成引用。
- **遇到问题**：测试恢复时向量仓储方法曾位于错误的 Fake 类中，导致服务测试收集/调用失败。
- **解决方式**：将测试方法归回 `FakeKnowledgeRepository`，不修改生产仓储接口；重新执行服务测试和编译检查。
- **验证结果**：`tests/test_knowledge_service.py` 为 `25/25` 通过；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 个人博客 RAG 与 Agent：任务 5 检索模式与引用快照阶段

- **完成时间**：20:49（Asia/Shanghai）
- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；通过注入查询 Embedding 执行向量模式；无结果不生成引用；检索结果转换为冻结的 `CitationSnapshot`，保留文档版本、片段、URL、章节、行号和正文快照。
- **遇到问题**：测试夹具中的向量仓储方法曾误插入 `FakeLLM`，并留下多余缩进导致测试收集失败。
- **解决方式**：将方法归回 `FakeKnowledgeRepository`，删除残留代码，重新执行目标测试和编译检查。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 21:00 任务 5：知识问答检索服务阶段恢复验证

- **完成内容**：确认 `KnowledgeService.retrieve()` 已统一分派关键词、向量和混合检索；向量模式通过注入查询 Embedding 获取向量；无结果返回 `no_results` 且不生成引用；检索结果转换为冻结的 `CitationSnapshot`，保留文档版本、来源、章节、行号和正文快照。
- **遇到问题**：恢复时曾怀疑测试夹具中的向量仓储方法归属错误。
- **解决方式**：重新读取完整测试文件，确认 `FakeKnowledgeRepository` 方法归属和生产服务实现均正确，无需重复修改。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 21:21 任务 5：知识问答服务检索分派与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；向量模式使用可注入查询 Embedding，禁止静默降级；检索结果转换为冻结的 `CitationSnapshot`，无结果返回 `no_results`。
- **遇到问题**：新增服务测试时，向量仓储方法曾残留在错误的测试替身中，导致测试文件出现未闭合方法定义。
- **解决方式**：将向量和混合方法归回 `FakeKnowledgeRepository`，删除残留定义并重新核对测试结构；未修改生产仓储接口。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 21:27 任务 5：知识问答服务检索分派与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；向量模式使用可注入查询 Embedding，禁止静默降级；检索结果转换为冻结的 `CitationSnapshot`，无结果返回 `no_results`。
- **遇到问题**：新增服务测试时，向量仓储方法暂时插入了错误的测试替身，导致测试收集/分派失败。
- **解决方式**：将向量和混合方法归回 `FakeKnowledgeRepository`，未修改生产接口。
- **验证结果**：`tests/test_knowledge_service.py` **25/25 通过**；相关编译检查通过；未调用真实模型或外部 Embedding 服务。

## 21:39 任务 5：知识问答服务三模式检索与引用快照阶段

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；向量模式通过注入查询 Embedding 获取查询向量，禁止静默降级；检索结果转换为冻结的 `CitationSnapshot`，无结果返回 `no_results`。
- **遇到问题**：补充服务测试时，测试替身中的仓储方法和 `FakeLLM` 定义发生错位，导致 9 个测试失败。
- **解决方式**：恢复 `FakeKnowledgeRepository` 与 `FakeLLM` 的类边界，未修改生产服务逻辑。
- **验证结果**：`tests/test_knowledge_service.py tests/test_knowledge_repository.py tests/test_knowledge_ingestion.py tests/test_retrieval.py` **51/51 通过**；相关 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 21:58 任务 5：知识问答服务检索分派与引用快照阶段

- **完成时间**：21:58（Asia/Shanghai）
- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；通过注入查询 Embedding 执行向量类模式；检索结果转换为冻结的 `CitationSnapshot`，保留文档版本、来源 URL、章节、行号和正文快照；无结果返回 `no_results`。
- **遇到问题**：测试夹具的仓储向量方法归属曾需要核对，可能造成服务测试调用失败。
- **解决方式**：确认向量和混合方法均属于 `FakeKnowledgeRepository`，未修改生产仓储接口。
- **验证结果**：`tests/test_knowledge_service.py` **26/26 通过**；知识服务相关编译检查通过；未调用真实模型或外部 Embedding 服务。

## 22:24 任务 5：知识问答检索分派与引用快照阶段验证

- **完成内容**：完成 `KnowledgeService.retrieve()` 的关键词、向量和混合检索分派；通过可注入查询 Embedding 执行向量模式；检索结果转换为引用快照，保留文档版本、来源、章节、行号和正文快照；空结果不生成引用。
- **遇到问题**：恢复上下文时发现测试替身的向量方法归属需要重新核对，生产实现未受影响。
- **解决方式**：确认并修正 `FakeKnowledgeRepository` 的三种检索方法归属，服务层继续禁止向量模式静默降级。
- **验证结果**：`tests/test_knowledge_service.py tests/test_embeddings.py tests/test_retrieval.py tests/test_knowledge_repository.py` **58/58 通过**；`app` 和相关测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 22:27 任务 5：知识问答检索服务阶段恢复验证

- **完成内容**：确认 `KnowledgeService.retrieve()` 已统一支持关键词、向量和混合检索；向量模式通过注入查询 Embedding 执行，检索结果转换为不可变引用快照，并保留文档版本、来源、章节、行号和正文快照。
- **遇到问题**：无新的实现阻碍；恢复过程中仅重新核对了测试替身与服务层接口。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_knowledge_service.py` **32/32 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 22:31 任务 5：知识问答模型生成与引用归属阶段

- **完成内容**：实现并验证 `KnowledgeService.answer()`；通过不可信证据提示词调用注入的 LLM，要求输出 `answer`、`citation_ids` 和 `evidence_status` JSON；仅允许引用本次检索获得的片段，拒绝虚构、重复或状态不一致的引用；无结果时不调用 LLM。
- **遇到问题**：无新的实现阻碍。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_knowledge_service.py` **32/32 通过**；`app/knowledge` 与服务测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 22:36 任务 5：知识问答 HTTP 闭环阶段

- **完成内容**：完成并核对知识问答 HTTP 入口、Session 事件持久化、消息历史接口和文档来源接口；知识问答结果持久化回答、检索方式、证据状态和引用快照。
- **遇到问题**：无新的实现阻碍；恢复上下文后确认相关路由和测试已存在。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_knowledge_service.py tests/test_knowledge_api.py` **36/36 通过**；`app`、知识服务和 API 测试 `compileall` 通过；未调用真实模型或外部 Embedding 服务。

## 23:10 任务 5：知识问答追问与引用快照阶段门

- **完成时间**：23:10（Asia/Shanghai）
- **完成内容**：验证知识问答服务的三种检索模式、追问查询改写、无结果不调用生成模型、引用归属校验、Session 事件持久化、消息历史和文档来源接口；追问同时保存原问题与检索问题，历史回答保留文档版本和正文快照。
- **遇到问题**：无。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_knowledge_service.py tests/test_knowledge_api.py tests/test_main.py tests/test_repository.py` **59/59 通过**；后端应用与相关测试 `compileall` 通过；未调用真实 LLM 或 Embedding 服务。

## 23:14 任务 5：知识问答 HTTP 阶段恢复确认

- **完成时间**：23:14（Asia/Shanghai）
- **完成内容**：确认知识问答 API 已提供知识 Run、Session 消息历史和文档来源接口；追问会记录原问题与改写检索问题，回答事件保存检索方式、证据状态和引用快照。
- **遇到问题**：无。
- **解决方式**：无需处理。
- **验证结果**：`tests/test_knowledge_service.py tests/test_knowledge_api.py tests/test_main.py tests/test_repository.py` **59/59 通过**；前端 `pnpm exec tsc --noEmit` 通过；未调用真实 LLM 或外部 Embedding 服务。
