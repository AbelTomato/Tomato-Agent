# 2026-09-07 开发日志

## 最小 Agent 可用性修复

### 用户 Prompt 原文

> 就最小Agent目标而言是否可用

### 处理摘要

- 将最小目标确定为：Runtime 可执行 LLM/工具闭环，HTTP API 实际调用 Runtime，终态 Run 不重复执行，Context 压缩不重复累积。
- `ContextState` 增加已压缩消息计数，修复 `recent_messages=0`，避免同一批消息重复进入摘要。
- Runtime 拒绝恢复 `completed`/`failed` 终态 Run；用户消息 Event 统一使用 `content` 字段。
- HTTP Run 接入 `AgentRuntime`；真实 LLM 仍通过 `LLMClient` 注入，未配置时返回明确失败结果，不伪造模型答案。
- README 和系统设计同步更新 Runtime 已实现、LLM Provider 需注入及 Run 恢复边界。

### 修改文件

- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\agent\models.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\agent\context.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\agent\runtime.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\main.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\tests\test_context.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\tests\test_runtime.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\tests\test_main.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\README.md`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\docs\系统设计.md`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\docs\development-log\2026-09-07.md`

### 验证

- 后端 pytest：`39 passed`。
- Backend `compileall`：通过。
- `git diff --check`：通过。
- 测试覆盖 Context 边界、摘要幂等、终态 Run 拒绝恢复、用户消息事件字段和 API Runtime 接入。

### 重点需 CR

1. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\agent\runtime.py`：审查终态恢复边界、异常持久化和真实 `LLMClient` 接入契约。
2. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\main.py`：审查全局 LLM 注入、默认未配置行为和 HTTP 错误响应边界。
3. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\agent\context.py`：审查摘要计数与 Event 重放顺序的一致性。

### Agent 自检

- 未引入 LangChain 或替代核心 Loop 的 Agent 框架。
- 未记录 API Key 或 Authorization Header。
- Agent 自检不等同于人工 CR。

### 人工 CR

待用户执行。

### 遗留风险

- 默认 `UnconfiguredLLM` 只能验证 Runtime 接线，真实模型 Provider 尚未接入。
- 同一 Session 跨多个新 Run 的历史上下文继承策略仍未实现，当前连续上下文应复用同一 `run_id`。
- Event 并发序号竞争和工具结果大小限制仍属于后续加固项。

### 时间

- `Asia/Shanghai`：`2026-09-07 16:41:17 +08:00`

## OpenAI Compatible 适配器测试与运行配置

### 用户 Prompt 原文

> 配置完成，现在完成测试及其他配置。

### 处理摘要

- 保留 `LLMClient` 与 Runtime 的供应商无关边界，完善 `OpenAICompatibleClient` 的测试 transport 注入、模型名和超时校验。
- 将 `httpx>=0.27` 从仅测试依赖提升为运行时依赖，确保实际 HTTP Provider 可启动。
- 使用 `httpx.MockTransport` 覆盖 OpenAI Compatible 请求转换、普通响应、工具调用响应、异常响应和 API Key 不泄露行为。

### 修改文件

- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\llm\compatible_client.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\main.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\pyproject.toml`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\tests\test_compatible_client.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\docs\development-log\2026-09-07.md`

### 验证

- 适配器测试：`8 passed`。
- 后端完整 pytest：`47 passed`。
- `compileall`：通过。
- `git diff --check`：通过。
- 测试逻辑覆盖请求路径/Header/JSON、消息和工具定义转换、工具参数 JSON 解析、Provider 错误、非法响应、参数校验及密钥不进入异常信息。

### 重点需 CR

1. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\llm\compatible_client.py`：审查不同 Compatible Provider 对工具调用字段、空 content 和错误响应格式的兼容性。
2. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\main.py`：审查全局客户端初始化、`.env` 配置缺失时的失败行为及密钥生命周期。
3. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\tests\test_compatible_client.py`：审查 MockTransport 对真实 HTTP 行为的覆盖边界及多工具调用未支持场景。

### Agent 自检

- 未记录 API Key 或 Authorization Header；异常信息仅包含 HTTP 状态码。
- 未引入 Agent 框架，Runtime 核心 Loop 未改写。
- 适配器当前仅承载 Provider 返回的第一个工具调用，与现有 `LLMResponse` 单工具调用模型一致。

### 人工 CR

待用户执行。

### 遗留风险

- 当前 `LLMResponse` 和 Runtime 仍不支持一次响应中的多个工具调用。
- 尚未对真实 Provider 发起联网验收；不同服务的工具调用兼容性仍需按实际模型验证。

### 时间

- `Asia/Shanghai`：`2026-09-07 22:58:15 +08:00`

## 修复前端本地请求 CORS 预检 405

### 用户 Prompt 原文

> 已停止错误进程，重新拉起后再请求，后端显示405响应码

### 处理摘要

- 确认 `OPTIONS /api/sessions` 返回 405，而直接 `POST /api/sessions` 返回 200，根因为后端未配置 CORS 预检处理。
- 为后端增加仅允许 `localhost:5173` 和 `127.0.0.1:5173` 的 CORS 配置，未开放通配 Origin。
- 为前端发送流程增加 HTTP 状态检查、异常捕获和失败信息展示，避免请求失败时永久停留在“处理中”。

### 修改文件

- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\main.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\tests\test_main.py`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\frontend\src\main.tsx`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\docs\development-log\2026-09-07.md`

### 验证

- 后端 pytest：`49 passed`。
- 前端 `pnpm build`：通过，TypeScript 和 Vite 构建成功。
- `compileall`：通过。
- `git diff --check`：通过。
- 实际 `OPTIONS /api/sessions`：`200 OK`，返回 `access-control-allow-origin: http://localhost:5173`。
- 测试逻辑覆盖允许本地 Origin 和拒绝未配置 Origin 的预检行为。

### 重点需 CR

1. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\app\main.py`：审查 CORS Origin、方法和 Header 白名单是否符合部署环境，避免后续误扩大跨域范围。
2. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\frontend\src\main.tsx`：审查 Session 创建失败、LLM 调用失败和重复点击时的状态隔离与错误展示。
3. `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\backend\tests\test_main.py`：审查 ASGI 级预检测试与实际浏览器请求行为的一致性。

### Agent 自检

- CORS 未使用 `allow_origins=["*"]`，未开启不必要的凭据跨域。
- 未记录 API Key、Authorization Header 或其他敏感运行时信息。

### 人工 CR

待用户执行。

### 遗留风险

- 前端仍使用硬编码 `http://127.0.0.1:8000`，部署到其他环境时需改为环境配置或 Vite 代理。
- `/runs` 仍同步等待真实 LLM Provider，Provider 不可达时会等待客户端超时后才显示失败。

### 时间

- `Asia/Shanghai`：`2026-09-07 23:31:59 +08:00`

## 整理工作区并分功能提交

### 用户 Prompt 原文

> 整理工作区改动，分commit提交

### 处理摘要

- 按 Runtime/持久化、OpenAI Compatible 适配器、前后端本地联调和文档四类功能拆分提交，避免形成混杂提交。
- 将运行生成的 `backend/data/` 排除出版本控制；缓存、虚拟环境和前端构建产物继续保持忽略。

### 修改文件

- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\.gitignore`
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\docs\development-log\2026-09-07.md`

### 验证

- 分组提交后检查每个 commit 的文件清单和提交标题。
- 提交前后运行后端 pytest、前端构建和 `git diff --check`；本轮不改变业务行为。

### 重点需 CR

- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\.gitignore`：检查本地数据库排除范围，避免误忽略应提交的迁移或初始化文件。
- `D:\AbelTomato_Files\Developer\Projects\Tomato-Agent\docs\development-log\2026-09-07.md`：检查分组依据、提交验收结果和 Prompt 原文是否可追溯。

### 人工 CR

待用户执行。

### 遗留风险

- 工作区原有历史日志修改与本轮功能改动一起纳入文档提交，若需更细历史拆分应依据具体基线重新整理。

### 时间

- `Asia/Shanghai`：`2026-09-08 08:53:48 +08:00`

### 提交记录

- `431e234`：`feat: implement agent runtime and persistence`
- `dee2117`：`feat: add OpenAI compatible llm client`
- `499ce73`：`fix: enable local frontend api integration`
- 本日志随最终 `docs: record development changes` 提交提交。

### 最终验收

- 后端完整 pytest：`49 passed`。
- 前端 `pnpm build`：通过。
- 提交后工作区无未提交改动；`backend/data/`、`.env`、缓存、虚拟环境和前端构建产物均未纳入版本控制。